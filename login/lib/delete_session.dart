import 'package:flutter/material.dart';

class deleteCurrentSession extends StatefulWidget {
  const deleteCurrentSession({super.key});

  @override
  State<deleteCurrentSession> createState() => _deleteCurrentSessionState();
}

class _deleteCurrentSessionState extends State<deleteCurrentSession> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("SignUp"),
    );
  }
}
